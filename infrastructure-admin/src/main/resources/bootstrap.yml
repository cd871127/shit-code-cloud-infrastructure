#应用个性化的配置
spring:
  profiles:
    active: dev
  boot:
    admin:
      discovery:
        ignored-services: consul
  cloud:
    consul:
      port: 8500
      config:
        format: FILES
        prefix: config/${spring.profiles.active}
        watch:
          enabled: true
          delay: 1
