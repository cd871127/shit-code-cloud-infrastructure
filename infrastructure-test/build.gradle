apply from: "${project.rootDir}/build.springboot.gradle"
apply from: "${project.rootDir}/build.redis.gradle"

version '0.1.0'

dependencies {
    implementation group: 'com.shit.code.cloud', name: 'shit-code-cloud-cache-spring-boot-starter', version: shitCodeLibVersion
}
