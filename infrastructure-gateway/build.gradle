apply from: "${project.rootDir}/spring.gradle"

version '0.1.0'

dependencies {
    implementation group: 'org.springframework.cloud', name: 'spring-cloud-starter-gateway'
    implementation group: 'de.codecentric', name: 'spring-boot-admin-starter-client', version: adminVersion
}

bootRun {
    setMain("com.shit.code.infrastructure.gateway.Main")
}