spring:
  application:
    name: infrastructure-gateway
  profiles:
    include: consul,database,admin
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true


