spring:
  application:
    name: infrastructure-gateway
  profiles:
    include: consul,database,admin,database1
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true


