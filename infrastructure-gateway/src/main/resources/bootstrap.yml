spring:
  application:
    name: infrastructure-gateway
  profiles:
    active: dev
    include: consul,database
  main:
    allow-bean-definition-overriding: true
  cloud:
    consul:
      port: 8500
      config:
        format: FILES
        prefix: config
#        prefix: config/${spring.profiles.active}
        watch:
          enabled: true
          delay: 1
        enabled: true
  zipkin:
    sender:
      type: rabbit
  boot:
    admin:
      client:
        auto-deregistration: true
        instance:
          prefer-ip: true
logging:
  config: classpath:logback-spring.xml
management:
  endpoint:
    refresh:
      enabled: true
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: '*'