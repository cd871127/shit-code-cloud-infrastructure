#个性化配置
spring:
  application:
    name: infrastructure-gateway
  profiles:
    active: dev
    include: admin
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: test
          uri: http://www.baidu.com/s/
          predicates:
            - Path=/*
          filters:
            - AddRequestParameter=wd, github
management:
  endpoints:
    web:
      exposure:
        include: health,status,gateway
