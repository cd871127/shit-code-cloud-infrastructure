<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shit.code.cloud.infrastructure.gateway.dao.mapper.RouteAccessoryMapper">

    <select id="selectList" resultType="com.shit.code.cloud.infrastructure.gateway.dao.dto.RouteAccessoryDTO">
        select name, args, type, route_id, unique_id, version, status, create_by, create_time, update_by, update_time
        from gateway_db.t_route_accessory
        <where>
            <if test="routeId!=null">route_id = #{routeId}</if>
            <if test="name!=null">and name = #{name}</if>
            <if test="type!=null">and type = #{type}</if>
            <if test="uniqueId!=null">and unique_id = #{uniqueId}</if>
        </where>
    </select>

    <insert id="insertList" parameterType="java.util.List">
        insert into gateway_db.t_route_accessory(name, args, type, route_id)
        value
        <trim suffixOverrides=",">
            <foreach collection="list" item="item">
                (#{item.name},#{item.args,jdbcType=VARCHAR},#{item.type},#{item.routeId}),
            </foreach>
        </trim>
    </insert>

    <insert id="insert" parameterType="RouteAccessoryDTO">
        insert into gateway_db.t_route_accessory(name, args, type, route_id)
        values (#{name}, #{args,jdbcType=VARCHAR}, #{type}, #{routeId})
    </insert>

</mapper>